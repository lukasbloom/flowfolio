/**
 * Storage-state path shared by playwright.config.ts (project `use` blocks)
 * and global-setup.ts (which writes the file). Relative to frontend/.
 */
export const STORAGE_STATE_PATH = "tests/e2e/.auth/storageState.json";
